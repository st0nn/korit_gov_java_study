package _16_Class.Car;

public class Car {
    String name;

    public Car(String carName){
        this.name = carName;
    }

    Car(String carName, int carYearModel, String carColor){
    //객체는 클래스가 없으면 생성 불가능, 하위 개념?

    //생성자
    //나는 Car()가 객체를 만드는 키워드인줄 알았는데 그게 아니라 Car()가 생성자이고 클래스를 불러오기만 하는거는 Car만 있으면 되는건지?
    // > 생성자는 객체를 만들때 필수조건이 아닌건지?
    //기본 생성자의 역활은 do while에서 do 와 동일한 역활인지?

    //this
    // this는 클래스는 덮어씌우는 역활인지?
    // this역활은 audi.name = ~~와 같은 역활인지?

    // 객체의 생성 조건이 궁금함
    // 생성자 = 메소드 생성이 아닌건지?





    }









}
